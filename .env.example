# MoonTV 本地配置示例
# 添加所有验收测试/本地启动必要配置项
# 只需复制为 .env 并完善具体内容即可
# 此文件不会被提交，只作为团队配置参考

# 站点名称（可选，默认 MoonTV）
SITE_NAME=MoonTV

# 公告（可选）
ANNOUNCEMENT=本网站仅提供影视信息搜索服务，所有内容均来自第三方网站。本网站不存储任何视频资源。

# 启用注册（true/false，默认 false）
NEXT_PUBLIC_ENABLE_REGISTER=false

# 搜索最大页数（默认 50）
NEXT_PUBLIC_SEARCH_MAX_PAGE=50

# 缓存时间（秒，默认 7200）
CACHE_TIME=7200

# 图像代理地址（如无需代理可留空）
NEXT_PUBLIC_IMAGE_PROXY=

# 豆瓣代理地址（如无需代理可留空）
NEXT_PUBLIC_DOUBAN_PROXY=

# 禁用黄色过滤（true/false，默认 false）
NEXT_PUBLIC_DISABLE_YELLOW_FILTER=false

# 存储类型（localstorage/redis 默认localstorage）
NEXT_PUBLIC_STORAGE_TYPE=localstorage

# 管理员用户名（可选）
USERNAME=

# Docker环境（默认false，可忽略）
DOCKER_ENV=false

### 其它自定义项可参考 src/lib/config.ts 字段
### 本文件字段变动需同步 config.ts 类型定义
